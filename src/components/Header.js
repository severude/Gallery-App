import React from 'react';
import Form from './Form';
import Navigation from './Navigation';

const Header = () => (
    <div>
        <Form />
        <Navigation />
    </div>
);

export default Header;